# 🩺 النظام الذكي للمساعدة الطبية (Medical NLP Assistant)

> **نظام محادثة طبي متكامل يعمل بخصوصية تامة دون اتصال بالإنترنت (Fully Offline RAG).**

![License](https://img.shields.io/badge/License-MIT-blue.svg) ![Python](https://img.shields.io/badge/Python-3.10%2B-green) ![Platform](https://img.shields.io/badge/Platform-Local-orange)

يُمثل هذا المشروع حلاً متقدماً لاستفسارات المرضى والأطباء، مبني على أحدث تقنيات **الذكاء الاصطناعي التوليدي (Generative AI)** مع تعزيز الدقة باستخدام **الاسترجاع المعرفي (RAG)**. تم تصميم النظام ليعمل محلياً بالكامل لضمان سرية البيانات الطبية وخصوصية المرضى.

---

## 🎥 عرض توضيحي (Demo)

شاهد كيف يعمل النظام، من طرح السؤال وحتى الحصول على الإجابة مع التوثيق الطبي:

<div align="center">
  <video src="assets/demo.mp4" width="100%" controls autoplay loop muted></video>
</div>

---

## ✨ المميزات الرئيسية

### 1. 🧠 ذكاء اصطناعي محلي مع "التفكير" (Reasoning)
يستخدم النظام نموذج `Qwen-4B` المخصص، القادر على تحليل الأعراض وتقديم إجابات منطقية، مع عرض **عملية التفكير** للمستخدم لتعزيز الشفافية والفهم.

### 2. 📚 نظام استرجاع معزز (RAG)
بدلاً من الاعتماد على ذاكرة النموذج فقط، يقوم النظام بالبحث في قاعدة بيانات طبية موثقة (`Medical Knowledge Base`) تحتوي على آلاف الأسئلة والأجوبة، لضمان دقة وموثوقية المعلومة.

### 3. ✅ مؤشرات الثقة (Confidence Scores)
نظام فريد يعرض لك **نسبة الثقة** في المعلومات المسترجعة (عالية، متوسطة، منخفضة) لتتمكن من تقييم جودة الإجابة بنفسك.

### 4. 🎨 واجهة مستخدم احترافية
واجهة عصرية مبنية بـ `Streamlit` مع تصميم **Glassmorphism**، تدعم الوضع الليلي (Dark Mode) بشكل تلقائي وتفاعلي، وتوفر تجربة مستخدم سلسة وجذابة.

### 5. 🔒 خصوصية تامة (Offline First)
لا يتم إرسال أي بيانات إلى خوادم خارجية (مثل OpenAI أو Google). كل شيء، من قاعدة البيانات إلى النموذج اللغوي، يعمل على جهازك الشخصي حمايةً للخصوصية.

---

## 🛠️ البنية التقنية (Architecture)

يتكون النظام من جزأين رئيسيين يعملان بتناغم:

| المكون | الوصف | التقنية المستخدمة |
| :--- | :--- | :--- |
| **Server (العقل)** | يدير النموذج اللغوي ويعالج الطلبات بسرعة عالية. | `Python`, `gRPC`, `vLLM` |
| **Client (الواجهة)** | يربط المستخدم بالنظام، ويدير عملية البحث (RAG) والعرض. | `Streamlit`, `ChromaDB`, `SentenceTransformers` |

---

## 🚀 دليل التشغيل السريع

اتبع هذه الخطوات لتشغيل النظام في أقل من دقيقة:

### المتطلبات المسبقة
- نظام تشغيل Linux/Mac أو Windows (مع WSL).
- بطاقة رسوميات NVIDIA (مفضل) أو CPU قوي (سيعمل أبطأ قليلاً).

### 1. تشغيل السيرفر (Server)
هذه الخطوة تشغل "المحرك" الخلفي. افتح نافذة Terminal واكتب:
```bash
python src/server/server.py
```
> ⏳ انتظر بضع ثوانٍ حتى تظهر رسالة: `🎧 السيرفر يستمع الآن على المنفذ...`

### 2. تشغيل الواجهة (UI)
افتح نافذة Terminal **جديدة** (لا تغلق السيرفر) واكتب:
```bash
streamlit run src/ui/main.py
```
سيفتح المتصفح تلقائياً وستظهر لك واجهة المحادثة.

---

## 📂 هيكل المشروع

```
.
├── dataset/                  # البيانات الطبية (AHQAD.csv)
├── chroma_db_storage/        # قاعدة البيانات المتجهية (Vector DB)
├── assets/                   # ملفات الوسائط (الفيديو والصور)
├── src/
│   ├── core/                 # المنطق الأساسي (Client, Config)
│   ├── server/               # كود السيرفر (gRPC Server)
│   └── ui/                   # واجهة المستخدم (Main, Components, CSS)
├── notebooks/                # دفاتر الجوبيتر (للتدريب والتقييم)
├── report.md                 # التقرير العلمي للمشروع
└── README.md                 # هذا الملف
```

---

## 🧪 التقييم والأداء

تم اختبار النظام على مجموعة بيانات اختبارية للتأكد من الجودة:
- **الملف**: `04_Evaluation.ipynb`
- **عدد الأسئلة**: 20 سؤال (15 طبي + 5 عام).
- **النتائج**: حقق النظام دقة استرجاع عالية وقدرة ممتازة على رفض الأسئلة خارج المجال الطبي.

💡 **لتشغيل التقييم بنفسك:** افتح ملف `04_Evaluation.ipynb` وشغل الخلايا لترى النتائج مباشرة.

---
*Created with ❤️ for Medical AI Advancement*
